const initialState = {};
const initialReducer = (state = initialState, action) => {
  //let newState;

  switch (action.type) {
    case '':
      break;
    default:
      return state;
  }
};

export default initialReducer;
